# Password Input App

## 📌 Description
This is a simple React application that allows users to enter a password and interact with a submit button.

The app demonstrates:
- Handling input events (`onChange`)
- Handling mouse events (`onMouseEnter`, `onMouseLeave`)
- Component-based structure in React

---

## 🚀 How to Run the Project

1. Install dependencies:
```bash
npm install